<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.music.dao.UserLoginDao">
        <select id="getUserLogin" parameterType="userLogin" resultType="UserLogin">

            select id,
                   user_count as userCount,
                   user_password as userPassword,
                   user_email as userEmail,
                   user_phone as userPhone,
                   user_register_date as userRegisterDate
            from user_login where user_count=#{userCount} and user_password=#{userPassword}

        </select>

    <select id="getUserAdmin" parameterType="UserLogin" resultType="Integer">
            select user_login_id
            from sys_admin where user_login_id = #{id}
    </select>


</mapper>